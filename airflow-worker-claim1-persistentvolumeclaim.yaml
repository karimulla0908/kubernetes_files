apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: airflow-claim1
  name: airflow-claim1
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 200Mi
